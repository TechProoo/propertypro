export default function SectionTwo() {
  return (
    <section className="bg-bg h-100 flex items-center justify-center">
      <div className="border-b border-border pb-4">
        <h1 className="text-text-heading text-[clamp(28px,5vw,50px)] font-bold max-sm:text-[28px]">
          We specialize in these fields.
        </h1>
      </div>
    </section>
  );
}
